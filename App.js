const heading = React.createElement("h1",{
    id : "heading"
},"welcome to the Namste React Course and this is the best react course now to learn !!");

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(heading)